[project]
name = "visacanada-backend"
version = "0.1.0"
description = "VisaCanada - AI-powered immigration management platform"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.9.0",
    "pydantic-settings>=2.5.0",
    "sqlalchemy[asyncio]>=2.0.35",
    "asyncpg>=0.30.0",
    "alembic>=1.13.0",
    "redis>=5.1.0",
    "celery>=5.4.0",
    "httpx>=0.27.0",
    "python-multipart>=0.0.12",
    "python-jose[cryptography]>=3.3.0",
    "passlib[bcrypt]>=1.7.4",
    "email-validator>=2.1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
    "httpx>=0.27.0",
    "ruff>=0.6.0",
    "aiosqlite>=0.20.0",
]

# Several importable packages live at the project root (app, scripts, tests), so
# setuptools' automatic discovery is ambiguous and fails the editable install.
# Only the application package is distributable; scripts and tests are not.
[tool.setuptools]
packages = ["app"]

[tool.ruff]
target-version = "py312"
line-length = 100
# Migration scripts are generated and then frozen: reformatting them adds churn
# to an append-only history without improving anything that gets maintained.
extend-exclude = ["alembic/versions"]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]
# The formatter still wraps code at line-length (100). This only relaxes the lint
# ceiling for what it cannot split: single string literals such as user-facing
# messages and __repr__, which lose readability when broken by hand.
pycodestyle.max-line-length = 120

[tool.ruff.lint.per-file-ignores]
# Seed files are IRCC reference data (program descriptions, document lists) held
# in long string literals. Wrapping them would obscure the content.
"app/seeds/*" = ["E501"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
